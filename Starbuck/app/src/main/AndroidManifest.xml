<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.starbuck">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AtHomeCoffeeActivity"></activity>
        <activity android:name=".FoodActivity" />
        <activity android:name=".DrinksActivity" />
        <activity android:name=".AtHomeCofeeCategoryActivity" />
        <activity android:name=".FoodCategoryActivity" />
        <activity android:name=".DrinksCategoryActivity" />
        <activity android:name=".TopLevelActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>